% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-tracks.R
\name{simulate_tracks}
\alias{simulate_tracks}
\title{Simulate surrogate tracks for an animal segment}
\usage{
simulate_tracks(
  animal,
  sim_n = 1000L,
  oceanmask = NULL,
  min_locs = 6L,
  anchor = c("start", "start_end"),
  cores = 1L,
  seed = NULL
)
}
\arguments{
\item{animal}{A track_tbl for a single segment. Requires columns: id, lon, lat, time.}

\item{sim_n}{Integer. Number of simulations.}

\item{oceanmask}{Optional terra::SpatRaster with values 0=ocean, 1=land.}

\item{min_locs}{Integer. If animal has < min_locs rows, it will be resampled to \code{min_locs}.}

\item{anchor}{One of c("start","start_end"). Fix first point only, or first+last.}

\item{cores}{Integer. Number of cores. On macOS/Linux uses fork parallelism.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A tibble with columns: animalID, simID, time, lon, lat
}
\description{
Fits a surrogate AR movement model (availability) to a single animal segment
and generates \code{sim_n} simulated tracks with the same timestamps.
Optionally constrains simulated points to sea using an ocean mask (terra).
}
